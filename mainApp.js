
define(['angular', 'angular-resource', 'angular-route'], function (angular) {
    // Declare app level module which depends on filters, and services
	// load module dependencies here!!
	// var mapCustomizerApp = angular.module("mapCustomizer", [
	// 			'ui.bootstrap',
	// 			'ngResource'
	// 		]
	// 	);

  return angular.module('mapCustomizer', ['ngResource', 'ngRoute']);
});

